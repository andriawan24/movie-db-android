<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.MovieDB" parent="Theme.Material3.DayNight.NoActionBar" />

    <style name="Theme.MovieDB" parent="Base.Theme.MovieDB" />

    <style name="Theme.MovieDB.TextInputLayout.OutlinedBox" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="boxCornerRadiusTopStart">@dimen/_40dp</item>
        <item name="boxCornerRadiusTopEnd">@dimen/_40dp</item>
        <item name="boxCornerRadiusBottomStart">@dimen/_40dp</item>
        <item name="boxCornerRadiusBottomEnd">@dimen/_40dp</item>
    </style>

</resources>